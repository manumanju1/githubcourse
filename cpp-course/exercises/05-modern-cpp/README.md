# Exercise 05: Modern C++

Topics:
- Smart pointers (`std::unique_ptr`, `std::shared_ptr`)
- Move semantics and `std::move`
- Range-based loops
- `constexpr`, `auto`, and `enum class`

Try:
- Rewrite a class to use smart pointers safely.
- Practice `auto` and ranged loops with containers.
